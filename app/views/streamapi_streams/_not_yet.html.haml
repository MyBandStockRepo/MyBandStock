= raw "This stream is not currently being broadcast. It is scheduled to start on <strong>#{ stream.starts_at.strftime('%a %b %d, %Y at %I:%M%p') }</strong>."
Once the host is live, you will automatically be taken to the stream.

:javascript
  // We refresh every minute.
  setInterval(function(){
    window.location.reload();
  }, 60000);

-# __commented__
  #viewer-container
    %h2
      = @stream.title + ' - ' + @stream.starts_at.strftime("%m/%d/%Y")
    This stream is not currently being broadcast. It is scheduled to start on
    %strong
      = @stream.starts_at.strftime('%a %b %d, %Y at %I:%M%p')
    \.
    %br
    %p
      If you believe you are receiving this message in error, please email
      = mail_to(MBS_SUPPORT_EMAIL, MBS_SUPPORT_EMAIL, :encode => "hex") + '.'

