#head_top
  = link_to root_path, {:id => 'header-logo-link'} do
    = image_tag('/images/header/mbs_logo.gif', :alt => 'MyBandStock')
  .loggedin
    .loggedin_leftborder
    #logged_info
      #user_email
        = user.email
      = link_to '', me_account_path(:id => user.id), :class => 'account'
    = link_to '', logout_path, :class => 'logout'

#head_bottom
  #logo_float
    = link_to '', root_path, :class => 'navlogo'
  -#
    #stock-ticker
      Stock ticker test

