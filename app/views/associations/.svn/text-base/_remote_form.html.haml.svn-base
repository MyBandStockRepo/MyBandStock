-remote_form_for(association) do |f|
  .form-entry
    %fieldset
      %legend 
        %strong
          Edit Association
      .form-fields
        =render :partial => 'associations/form', :locals => {:f=>f }
        .form-actions
          =image_submit_tag 'buttons/global/btn-update.png', :class => 'submit'
