<h1>Listing share_codes</h1>

<table>
  <tr>
    <th>Key</th>
    <th>Share code group</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @share_codes.each do |share_code| %>
  <tr>
    <td><%= share_code.key %></td>
    <td><%= share_code.share_code_group_id %></td>
    <td><%= link_to 'Show', share_code %></td>
    <td><%= link_to 'Edit', edit_share_code_path(share_code) %></td>
    <td><%= link_to 'Destroy', share_code, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Share code', new_share_code_path %>
