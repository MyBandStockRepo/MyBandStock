%h1="#{@band.name} - Live Stream Series"
-for series in @live_stream_series
  %h2= series.name
    -for stream in series.streamapi_streams
      %ul
        %li
          ="Begins - #{stream.begins_at}"
        %li
          ="Ends - #{stream.ends_at}"
          